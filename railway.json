{
  "builder": "nixpacks",
  "deploy": {
    "preDeployCommand": "cd laravel && php artisan migrate --force",
    "healthcheckPath": "/",
    "restartPolicy": {
      "type": "on-failure",
      "maxRetries": 10
    }
  }
}