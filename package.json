{
  "name": "mandapos-landing",
  "version": "1.0.0",
  "private": true,
  "description": "Bootstrap scripts for running Laravel app in /laravel",
  "main": "server.js",
  "scripts": {
    "start": "cd laravel && php artisan serve --host=0.0.0.0 --port=${PORT:-8000}",
    "dev": "cd laravel && php artisan serve"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "express-session": "^1.18.1",
    "express": "^4.21.2",
    "pg": "^8.13.1"
  }
}